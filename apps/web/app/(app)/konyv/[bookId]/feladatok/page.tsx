import { ScreenPlaceholder } from "@/components/shell";

/** AI feladatok (AI jobs queue) placeholder — filled in M10 (V1). */
export default function FeladatokPage() {
  return (
    <ScreenPlaceholder
      label="AI feladatok"
      hint="Az AI feladatok az M10-ben érkezik."
    />
  );
}

import { ScreenPlaceholder } from "@/components/shell";

/** Idősor (Timeline) placeholder — filled in M10 (V1). */
export default function IdosorPage() {
  return <ScreenPlaceholder label="Idősor" hint="Az Idősor az M10-ben érkezik." />;
}

import { ScreenPlaceholder } from "@/components/shell";

/** Export placeholder — Markdown export lands in M8. */
export default function ExportPage() {
  return <ScreenPlaceholder label="Export" hint="Az Export az M8-ban érkezik." />;
}

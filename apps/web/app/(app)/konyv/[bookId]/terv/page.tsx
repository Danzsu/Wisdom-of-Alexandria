import { ScreenPlaceholder } from "@/components/shell";

/** Terv (Plan board) placeholder — filled in M7. */
export default function TervPage() {
  return <ScreenPlaceholder label="Terv" hint="A Plan Board az M7-ben érkezik." />;
}

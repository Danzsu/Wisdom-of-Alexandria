import { ScreenPlaceholder } from "@/components/shell";

/** Chat placeholder — filled in M10 (V1). */
export default function ChatPage() {
  return <ScreenPlaceholder label="Chat" hint="A Chat az M10-ben érkezik." />;
}

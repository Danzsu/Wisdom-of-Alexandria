# Shell scripts must stay LF: they are COPY'd into Linux containers
# (apps/api/docker-entrypoint.sh) or executed by git's sh (.husky hooks),
# and CRLF line endings break `sh` even when the content is POSIX-clean.
*.sh text eol=lf
.husky/* text eol=lf
